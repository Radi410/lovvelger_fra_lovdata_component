# Dockerfile.proxy
FROM node:20-alpine

WORKDIR /app

# Copy proxy server files
COPY proxy-server.js package.json ./

# Install only production dependencies
RUN npm install express cors node-fetch cheerio --omit=dev

EXPOSE 3001

CMD ["node", "proxy-server.js"]